import React from 'react';

class Form extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        const availableTime = this.getAvailableTime();

        return (
            <form>
                
            </form>
        )
    }
};

export default Form;